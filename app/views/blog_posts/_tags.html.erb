<% if @blog_post.blog_post_tags.any? %>
    <div class="col-lg-4">
      <div class="well">
        <h4>Tags: </h4>
        <% @blog_post.blog_post_tags.each do |tag| %>
            <a class="btn btn-primary" href="tags/<%= tag.tag.name %>">
              <%= tag.tag.name %>
            </a>
        <% end %>
      </div>
    </div>
<% end %>