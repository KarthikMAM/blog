<nav class="navbar navbar-inverse navbar-tall navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#navigator">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <a id="name" href="/" class="navbar-brand">Karthik M A M</a>
    </div>

    <div class="collapse navbar-collapse" id="navigator">
      <ul class="nav navbar-nav navbar-right">
        <%- %w[Projects Blog About Contact].each do |menu| %>
            <li class="<%= title[menu] == menu ? 'active' : '' %>">
              <%= link_to menu != '#' ? menu : '', eval("#{menu.downcase}_path") %>
            </li>
        <%- end %>
      </ul>
    </div>
  </div>
</nav>